{
  "name": "json-api-route-definition",
  "version": "3.0.0",
  "description": "",
  "main": "dist/src/main/index.js",
  "types": "dist/src/main/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "(rm -r ./dist || true) && tsc -d"
  },
  "author": "anyhowstep",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/AnyhowStep/json-api-route-definition"
  },
  "dependencies": {
    "@anyhowstep/data-validation": "^1.15.0",
    "@anyhowstep/json-api-schema": "^2.1.0",
    "schema-decorator": "^2.8.3"
  },
  "devDependencies": {
    "typescript": "^3.0.1"
  }
}
